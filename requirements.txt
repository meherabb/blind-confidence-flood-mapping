# Core deep learning
torch>=2.0
segmentation-models-pytorch>=0.3

# Geospatial I/O
rasterio>=1.3

# Scientific stack
numpy>=1.24
pandas>=1.5
scipy>=1.10
matplotlib>=3.7

# Utilities
tqdm>=4.65

# --- Notes ---
# The notebook auto-installs rasterio and segmentation-models-pytorch on first run
# if they are not already present, so a fresh Colab/Kaggle T4 runtime with only
# torch preinstalled is sufficient to get started.
#
# Tested against: Python 3.10+, CUDA-enabled T4 GPU (Colab/Kaggle free tier).
# The pipeline also runs on CPU in DRY_RUN mode (synthetic data, no GPU required).
